pragma solidity ^0.5.0;

contract TodoList {
  uint public taskCount = 0; 
  //state variable, variable stored on blockchain, public then gives us a function to read the variables (provided by solidity)
}